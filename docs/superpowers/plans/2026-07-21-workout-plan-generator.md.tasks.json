{
  "planPath": "docs/superpowers/plans/2026-07-21-workout-plan-generator.md",
  "tasks": [
    {
      "id": 1,
      "subject": "Task 1: Dev harness — tools/app-shim.js",
      "status": "completed",
      "description": "**Goal:** A reusable module that loads the app's script block headlessly and returns its live globals, so every other tool reads real program data.\n\n**Files:**\n- Create: `tools/app-shim.js`\n- Create: `tools/app-shim.test.js`\n- Create: `.gitignore`\n\n**Acceptance Criteria:**\n- [ ] `loadApp()` returns `PROGRAMS` with 2 entries whose ids are `meso1`, `meso2`\n- [ ] `loadApp()` returns `EXERCISE_ALTERNATIVES` as a non-empty object\n- [ ] `loadApp({ storage: { hypertrophy_program: '1' } })` seeds localStorage before boot\n- [ ] Repeated `loadApp()` calls do not leak globals between loads\n- [ ] `data/` is gitignored\n\n**Verify:** `node --test tools/app-shim.test.js` → all tests pass\n\n**Steps:** Write failing test; implement extractScript + DOM/canvas/localStorage shim + loadApp with global save/restore; create .gitignore; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/app-shim.js\", \"tools/app-shim.test.js\", \".gitignore\"], \"verifyCommand\": \"node --test tools/app-shim.test.js\", \"acceptanceCriteria\": [\"loadApp returns PROGRAMS with ids meso1, meso2\", \"EXERCISE_ALTERNATIVES is non-empty\", \"storage seeding works before boot\", \"no global leakage between loads\", \"data/ is gitignored\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 2,
      "subject": "Task 2: Fix boot-time program desync in index.html",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Make `boot()` sync `DAYS`/`MESOCYCLE`/`WEEK_PHASES`/`PROTOCOL_ITEMS` to the saved program, so a non-zero saved program renders its own days.\n\nPre-existing bug verified headlessly during planning: with `hypertrophy_program = 1` the masthead reads \"Mesocycle 2\" while `DAYS[0].exercises[0].id` is meso1's `incline_db_press`. Auto-select (Task 3) cannot work until this is fixed.\n\n**Files:**\n- Modify: `index.html` — add `syncProgramGlobals()` near `switchProgram()` (~line 1020), reuse inside `switchProgram()` (lines 1024–1030), call from `boot()` (~line 2447)\n- Create: `tools/program-sync.test.js`\n\n**Acceptance Criteria:**\n- [ ] With `hypertrophy_program = '1'`, `DAYS[0].exercises[0].id === 'm2_incline_smith'`\n- [ ] With no saved program, `DAYS[0].exercises[0].id === 'incline_db_press'`\n- [ ] `switchProgram()` still updates all four globals (no regression)\n- [ ] `node --check` on the extracted script passes\n\n**Verify:** `node --test tools/program-sync.test.js` → all tests pass\n\n**Steps:** Write failing test; add syncProgramGlobals(); call it from switchProgram and boot; run tests + node --check; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"index.html\", \"tools/program-sync.test.js\"], \"verifyCommand\": \"node --test tools/program-sync.test.js\", \"acceptanceCriteria\": [\"saved program 1 yields m2_incline_smith in DAYS\", \"default program yields incline_db_press\", \"switchProgram still updates globals\", \"node --check passes on extracted script\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 3,
      "subject": "Task 3: autoSelectNewProgram() in index.html",
      "status": "completed",
      "blockedBy": [
        2
      ],
      "description": "**Goal:** On first load after a new program is appended, switch to it automatically; otherwise leave the user's manual choice alone.\n\n**Files:**\n- Modify: `index.html` — add `autoSelectNewProgram()` after `saveProgram()` (~line 987), call from `boot()`\n- Create: `tools/auto-select.test.js`\n\n**Acceptance Criteria:**\n- [ ] Fresh install (no `hypertrophy_seen_programs`) records all ids and does NOT override the saved program\n- [ ] When an unseen program id exists, `currentProgramIdx` becomes its index and `hypertrophy_program` is persisted\n- [ ] After auto-select, `hypertrophy_seen_programs` contains every current id\n- [ ] A second load with no new programs leaves the manual selection intact\n- [ ] Corrupt `hypertrophy_seen_programs` JSON is treated as absent, not thrown\n\n**Verify:** `node --test tools/auto-select.test.js` → all tests pass\n\n**Steps:** Write failing test; add SEEN_PROGRAMS_KEY + autoSelectNewProgram(); call from boot between loadProgram and syncProgramGlobals; run tests + node --check; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"index.html\", \"tools/auto-select.test.js\"], \"verifyCommand\": \"node --test tools/auto-select.test.js tools/program-sync.test.js\", \"acceptanceCriteria\": [\"fresh install records ids without hijacking selection\", \"unseen program is auto-selected and persisted\", \"seen list updated to all ids\", \"manual selection sticks when nothing is new\", \"corrupt seen-programs JSON tolerated\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 4,
      "subject": "Task 4: Muscle map and volume landmarks",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Machine-readable data for per-muscle volume accounting: which muscles each exercise trains, and what weekly set counts are appropriate.\n\n**Files:**\n- Create: `tools/muscle-map.json` (66 program exercise ids; primary 1.0, secondary 0.5)\n- Create: `tools/volume-landmarks.json` (16 muscles, mev/mavLow/mavHigh/mrv)\n- Create: `tools/muscle-map.test.js`\n\n**Acceptance Criteria:**\n- [ ] Every exercise id in any `PROGRAMS[].days[].exercises[]` has a map entry\n- [ ] Every muscle named in the map exists in `volume-landmarks.json`\n- [ ] All credit values are numbers in (0, 1]\n- [ ] Landmarks satisfy `mev <= mavLow <= mavHigh <= mrv` for every muscle\n\n**Verify:** `node --test tools/muscle-map.test.js` → all tests pass\n\n**Steps:** Write failing test; write both JSON files (full contents in the plan document); run tests; commit. Note: `alt_*` swap-target ids are deliberately NOT mapped — they inherit the replaced exercise's profile via Task 5's fallback.\n\n```json:metadata\n{\"files\": [\"tools/muscle-map.json\", \"tools/volume-landmarks.json\", \"tools/muscle-map.test.js\"], \"verifyCommand\": \"node --test tools/muscle-map.test.js\", \"acceptanceCriteria\": [\"every program exercise mapped\", \"every mapped muscle has landmarks\", \"credits in (0,1]\", \"landmarks ordered mev<=mavLow<=mavHigh<=mrv\"], \"modelTier\": \"mechanical\"}\n```"
    },
    {
      "id": 5,
      "subject": "Task 5: analyze-history.js — export loading and per-exercise metrics",
      "status": "completed",
      "blockedBy": [
        1,
        4
      ],
      "description": "**Goal:** Normalize any export version into one shape and compute per-exercise adherence, progression, and effort statistics.\n\n**Files:**\n- Create: `tools/analyze-history.js`\n- Create: `tools/fixtures/export-v3.json`, `tools/fixtures/export-v2.json`\n- Create: `tools/analyze-history.test.js`\n\n**Acceptance Criteria:**\n- [ ] `normalizeExport` maps v1, v2, v3 to `{ programs: { <id>: { weeks: {...} } } }`\n- [ ] v2 payloads are attributed to `meso1` (matching the app's migration)\n- [ ] An unrecognized payload throws a clear error\n- [ ] `perExercise` reports completed, skipped, skipRate, weeksTouched, firstWeight, lastWeight, slope, effortCounts, swappedTo\n- [ ] `slope` is least-squares over week→weight, `null` with fewer than 2 weighted weeks\n- [ ] `findNewestExport` returns newest `*.json` by mtime, `null` if empty\n\n**Verify:** `node --test tools/analyze-history.test.js` → all tests pass\n\n**Steps:** Write fixtures (a stall, a fully-skipped exercise, a swap); write failing test; implement findNewestExport/normalizeExport/slopeOf/perExercise; run tests; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/analyze-history.js\", \"tools/analyze-history.test.js\", \"tools/fixtures/export-v3.json\", \"tools/fixtures/export-v2.json\"], \"verifyCommand\": \"node --test tools/analyze-history.test.js\", \"acceptanceCriteria\": [\"normalizeExport handles v1/v2/v3\", \"v2 attributed to meso1\", \"garbage payload throws\", \"perExercise reports all nine metrics\", \"slopeOf is least-squares and null under 2 points\", \"findNewestExport picks newest json\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 6,
      "subject": "Task 6: analyze-history.js — muscle volume, flags, and the report",
      "status": "completed",
      "blockedBy": [
        5
      ],
      "description": "**Goal:** Turn per-exercise statistics into weekly per-muscle volume, actionable flags, and a markdown report the command reads.\n\n**Files:**\n- Modify: `tools/analyze-history.js` (append functions and CLI entry point)\n- Modify: `tools/analyze-history.test.js` (append tests)\n\n**Acceptance Criteria:**\n- [ ] `resolveMuscles` uses a swap target's own entry when present, else the original exercise's profile\n- [ ] `weeklyVolume` credits completed sets only, weighted by muscle credit, averaged over weeks with data\n- [ ] `flagExercises` marks `rejected` at skipRate > 0.40 OR a swap in a majority of trained weeks\n- [ ] `flagExercises` marks `stalled` at slope <= 0 across 4+ weighted weeks with any high effort\n- [ ] `flagExercises` marks `underStimulating` when effort is majority low and slope <= 0\n- [ ] `flagVolume` reports muscles below mev and above mrv\n- [ ] `renderReport` emits `## Adherence`, `## Per exercise`, `## Weekly volume by muscle`, `## Flags`\n- [ ] CLI on the fixture prints a report and exits 0\n\n**Verify:** `node --test tools/analyze-history.test.js && node tools/analyze-history.js tools/fixtures/export-v3.json | head -40`\n\n**Steps:** Append failing tests; implement resolveMuscles/weeklyVolume/flagExercises/flagVolume/analyze/renderReport/main with named thresholds (REJECT_SKIP_RATE 0.40, STALL_MIN_WEEKS 4, LOW_EFFORT_SHARE 0.5); run tests + CLI; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/analyze-history.js\", \"tools/analyze-history.test.js\"], \"verifyCommand\": \"node --test tools/analyze-history.test.js && node tools/analyze-history.js tools/fixtures/export-v3.json | head -40\", \"acceptanceCriteria\": [\"resolveMuscles falls back to the original exercise\", \"weeklyVolume credits completed sets only\", \"rejected/stalled/underStimulating flags correct\", \"flagVolume compares against landmarks\", \"renderReport emits all four sections\", \"CLI exits 0 on the fixture\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 7,
      "subject": "Task 7: tools/smoke-render.js",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** A headless check that a given program renders every view without throwing — the gate that catches runtime errors `node --check` cannot.\n\n**Files:**\n- Create: `tools/smoke-render.js`\n- Create: `tools/smoke-render.test.js`\n\n**Acceptance Criteria:**\n- [ ] `smokeRender(htmlPath, programIdx)` renders every day plus plan, progress, settings\n- [ ] Returns `{ ok: true, rendered: [...] }` when all views produce non-empty `#scroll` markup\n- [ ] Returns `{ ok: false, error }` instead of throwing when a view fails\n- [ ] CLI exits 0 on success, 1 on failure, printing which view failed\n- [ ] Passes against the current `index.html` for both existing programs\n\n**Verify:** `node --test tools/smoke-render.test.js && node tools/smoke-render.js index.html 1`\n\n**Steps:** Write failing test; implement smokeRender using loadApp + cached #scroll element, iterating days then plan/progress/settings; add CLI; run tests; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/smoke-render.js\", \"tools/smoke-render.test.js\"], \"verifyCommand\": \"node --test tools/smoke-render.test.js && node tools/smoke-render.js index.html 1\", \"acceptanceCriteria\": [\"every program renders every view\", \"non-empty scroll markup per view\", \"failures returned not thrown\", \"CLI exit codes 0/1 correct\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 8,
      "subject": "Task 8: insert-program.js — validation gates",
      "status": "completed",
      "blockedBy": [
        1
      ],
      "description": "**Goal:** Reject a malformed or colliding program before any file is touched.\n\n**Files:**\n- Create: `tools/insert-program.js` (validation half only)\n- Create: `tools/fixtures/program-valid.json`\n- Create: `tools/insert-program.test.js`\n\n**Acceptance Criteria:**\n- [ ] `validateProgram` requires name, subtitle, totalWeeks, days, protocolItems, mesocycle, weekPhases\n- [ ] Rejects `weekPhases.length !== totalWeeks`\n- [ ] Rejects a day with no exercises\n- [ ] Rejects an exercise missing any of id, name, sets, reps, rpe, note, llp, compound, rest, restLabel, muscles\n- [ ] Rejects non-numeric `rest` and empty `muscles`\n- [ ] Rejects duplicate ids within the program and collisions with existing ids (program exercises AND EXERCISE_ALTERNATIVES targets)\n- [ ] Returns every error found, not just the first\n\n**Verify:** `node --test tools/insert-program.test.js` → all tests pass\n\n**Steps:** Write the complete two-day fixture; write failing test; implement collectExistingIds + validateProgram accumulating all errors; run tests; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/insert-program.js\", \"tools/insert-program.test.js\", \"tools/fixtures/program-valid.json\"], \"verifyCommand\": \"node --test tools/insert-program.test.js\", \"acceptanceCriteria\": [\"required top-level fields enforced\", \"weekPhases length must equal totalWeeks\", \"empty day rejected\", \"incomplete exercise rejected\", \"rest type and muscles emptiness checked\", \"duplicate and colliding ids rejected\", \"all errors returned at once\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 9,
      "subject": "Task 9: insert-program.js — splice, gate, and write",
      "status": "completed",
      "blockedBy": [
        7,
        8
      ],
      "description": "**Goal:** Insert a validated program into `index.html`, but only after `node --check` and a smoke render pass against the candidate file.\n\n**Files:**\n- Modify: `tools/insert-program.js` (append serialization, splice, gates, CLI)\n- Modify: `tools/insert-program.test.js` (append tests)\n\n**Acceptance Criteria:**\n- [ ] `nextProgramId` returns `meso3` for the current file, `meso4` after one insert\n- [ ] `toJsLiteral` emits unquoted identifier keys and preserves em/en-dashes\n- [ ] `spliceProgram` inserts before the PROGRAMS terminator and merges alternatives into EXERCISE_ALTERNATIVES\n- [ ] Splice anchors asserted unique; missing or duplicated anchor is a hard error\n- [ ] `insertProgram` writes nothing when validation, `node --check`, or smoke render fails\n- [ ] On success the file is replaced atomically and the new program renders\n- [ ] CLI exits 0 on success, 1 on any gate failure, printing which gate failed\n\n**Verify:** `node --test tools/*.test.js` → all tests pass, and `git status --short` shows index.html unmodified (tests use temp copies)\n\n**Steps:** Append failing tests using temp copies of index.html; implement nextProgramId/quote/toJsLiteral/spliceProgram (anchors `\\n];\\n\\nconst EXERCISE_ALTERNATIVES = {` and `\\n};\\n\\nlet DAYS = PROGRAMS[0].days;`, both verified unique) and insertProgram with four gates + atomic rename; add CLI; run full suite; commit. Full code in the plan document.\n\n```json:metadata\n{\"files\": [\"tools/insert-program.js\", \"tools/insert-program.test.js\"], \"verifyCommand\": \"node --test tools/*.test.js && git status --short\", \"acceptanceCriteria\": [\"nextProgramId follows mesoN sequence\", \"toJsLiteral unquotes identifier keys and keeps unicode\", \"spliceProgram merges program and alternatives\", \"anchor uniqueness enforced\", \"no write on any gate failure\", \"successful insert renders under smoke test\", \"CLI exit codes correct\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 10,
      "subject": "Task 10: docs/training-evidence.md",
      "status": "completed",
      "blockedBy": [
        4
      ],
      "description": "**Goal:** The researched rulebook the generator reasons from, plus a landmarks file that provably matches it.\n\n**Files:**\n- Create: `docs/training-evidence.md`\n- Modify: `tools/volume-landmarks.json` (revise values to match the research)\n- Create: `tools/evidence-sync.test.js`\n\n**Acceptance Criteria:**\n- [ ] Covers all eight topics: volume landmarks, frequency, proximity to failure, lengthened-position bias, rep ranges, progression models, deload timing, exercise rotation\n- [ ] Every quantitative claim carries an inline source link (at least 8 links)\n- [ ] Contains a `## Volume landmarks` table with one row per muscle key in `volume-landmarks.json`\n- [ ] `volume-landmarks.json` values match the table exactly\n- [ ] A `## Shoulder-cautious constraints` section states which rules always survive\n- [ ] A `## Last researched` line records the date\n\n**Verify:** `node --test tools/evidence-sync.test.js` → all tests pass\n\n**Steps:** Write the sync test first; run the eight WebSearch queries listed in the plan and WebFetch the strongest sources (prefer meta-analyses; record ranges where sources disagree); write the doc; reconcile volume-landmarks.json in the same commit; run evidence-sync + muscle-map tests; commit.\n\n```json:metadata\n{\"files\": [\"docs/training-evidence.md\", \"tools/volume-landmarks.json\", \"tools/evidence-sync.test.js\"], \"verifyCommand\": \"node --test tools/evidence-sync.test.js tools/muscle-map.test.js\", \"acceptanceCriteria\": [\"all eight topics covered\", \"at least 8 inline source links\", \"landmarks table has one row per muscle key\", \"table values match volume-landmarks.json exactly\", \"shoulder-cautious constraints section present\", \"last researched date present\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 11,
      "subject": "Task 11: .claude/commands/newplan.md",
      "status": "pending",
      "blockedBy": [
        3,
        6,
        9,
        10
      ],
      "description": "**Goal:** The orchestrator prompt that ties the helpers together and does the design work.\n\n**Files:**\n- Create: `.claude/commands/newplan.md`\n\n**Acceptance Criteria:**\n- [ ] Stops with export instructions when `data/` holds no `*.json`\n- [ ] Supports `--research` to refresh `docs/training-evidence.md` and the landmarks file\n- [ ] Asks exactly three multiple-choice questions before generating\n- [ ] Prints the approval brief and waits before calling the inserter\n- [ ] Never edits `index.html` directly — insertion goes through `tools/insert-program.js`\n- [ ] Leaves the change uncommitted and offers to commit\n\n**Verify:** `test -f .claude/commands/newplan.md && grep -c 'insert-program.js' .claude/commands/newplan.md` → at least 1\n\n**Steps:** Write the command file with the seven-step flow (find export → analyze → read rulebook → three questions → design → brief and wait → insert); verify; run full test suite; commit. Full command text in the plan document.\n\n```json:metadata\n{\"files\": [\".claude/commands/newplan.md\"], \"verifyCommand\": \"test -f .claude/commands/newplan.md && grep -c 'insert-program.js' .claude/commands/newplan.md && node --test tools/*.test.js\", \"acceptanceCriteria\": [\"stops with export instructions when data/ is empty\", \"--research refresh mode supported\", \"exactly three run-time questions\", \"approval brief printed before any write\", \"insertion only via tools/insert-program.js\", \"change left uncommitted, commit offered\"], \"modelTier\": \"standard\"}\n```"
    },
    {
      "id": 12,
      "subject": "Task 12: End-to-end verification with real data",
      "status": "pending",
      "blockedBy": [
        11
      ],
      "description": "**Goal:** Prove the whole pipeline works on the user's actual export, from `/newplan` through to the app rendering the new program.\n\n**USER-ORDERED GATE — NON-SKIPPABLE.** This task was requested by the user in the current conversation. It MUST NOT be closed by walking around it, by declaring it \"verified inline\", or by substituting a cheaper check. Close only after every item in `acceptanceCriteria` has been re-validated independently, with output captured.\n\n**Files:**\n- Modify: `index.html` (via `tools/insert-program.js` only — never by hand)\n- Create: `data/<user export>.json` (gitignored, supplied by the user)\n\n**Acceptance Criteria:**\n- [ ] `node tools/analyze-history.js` runs against the user's real export and exits 0\n- [ ] `/newplan` completes end-to-end and prints the approval brief\n- [ ] `node tools/insert-program.js newplan.json` exits 0 and reports `meso3`\n- [ ] `node tools/smoke-render.js index.html 2` exits 0\n- [ ] `node --test tools/*.test.js` passes with the new program in place\n- [ ] `git diff --stat index.html` shows additions only — no lines removed from meso1/meso2\n\n**Verify:** `node --test tools/*.test.js && node tools/smoke-render.js index.html 2 && git diff --stat index.html`\n\n**Steps:** Ask the user for a real export in `data/`; run the analyzer alone and sanity-check its numbers against what the user says about their training; run `/newplan` end-to-end; capture gate evidence; ask the user to confirm auto-select and intact meso1 history in a real browser; commit only once approved.\n\n```json:metadata\n{\"files\": [\"index.html\", \"data/\"], \"verifyCommand\": \"node --test tools/*.test.js && node tools/smoke-render.js index.html 2 && git diff --stat index.html\", \"acceptanceCriteria\": [\"analyze-history exits 0 on the real export\", \"/newplan prints the approval brief\", \"insert-program exits 0 reporting meso3\", \"smoke-render exits 0 for program index 2\", \"node --test tools/*.test.js passes\", \"git diff shows additions only, meso1/meso2 untouched\"], \"userGate\": true, \"tags\": [\"user-gate\"], \"modelTier\": \"standard\"}\n```"
    }
  ],
  "lastUpdated": "2026-07-21"
}
